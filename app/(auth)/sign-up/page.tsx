import React from "react";
/**
 * When you create route groups, they are not added to the file based routing, they're just used to create layouts and meaningful routing
 *Only folders within them that do not have paranthesis, count as additional routes

 */

const SignUp = () => {
  return <div>Sign up</div>;
};

export default SignUp;
